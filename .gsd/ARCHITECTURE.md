# Architecture

> Auto-generated by /map on 2026-02-24

## Overview

BharatQA Frontend is a React application providing a user interface for testers and administrators to manage bugs, payments, and testing activities. It appears to be a single-page application (SPA) built with `create-react-app` based on the dependencies.

```
┌─────────────────────────────────────────┐
│              [Entry Point]              │
│               src/index.js              │
├─────────────────────────────────────────┤
│         [Routing & Layout Layer]        │
│                src/App.js               │
├─────────────────────────────────────────┤
│         [Business Logic Layer]          │
│     src/pages/* & src/components/*      │
├─────────────────────────────────────────┤
│            [Data Layer]                 │
│           src/utils/api.js              │
└─────────────────────────────────────────┘
```

## Components

### Pages
- **Purpose:** Serve as the main routed views for the application.
- **Location:** `src/pages/`
- **Known Pages:** Includes `DashboardPage.css` (and likely corresponding JS/JSX files).

### Components
- **Purpose:** Reusable UI elements building up the pages.
- **Location:** `src/components/`
- **Subdirectories:** Includes `auth` and 14 other component directories.

### API Utilities
- **Purpose:** Centralized data fetching and external communication.
- **Location:** `src/utils/api.js`
- **Dependencies:** Uses native `fetch`.

## Data Flow

1. **User Interaction:** User interacts with UI components in `src/pages/` or `src/components/`.
2. **API Call:** Components invoke functions defined in `src/utils/api.js` to fetch or mutate data.
3. **Network Request:** `api.js` utilizes standard `fetch` API to communicate with the backend.
4. **State Update:** Data returned from the backend updates the component's internal state (likely via React hooks), triggering a re-render.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| Backend API | Internal API | Core application data fetching and mutation (Bugs, Testers, Payments, etc.) |

## Technical Debt

- [ ] Further analysis required: The `src/` directory contains many files not fully inspected (15 component directories, 16 page directories).
- [ ] No explicit third-party service integrations (Stripe, SendGrid, etc.) were found in the standard front-end code search, though they might be handled entirely backend-side.
- [ ] No `TODO` or `FIXME` comments found in the standard search, indicating either a clean codebase or lack of inline documentation for pending tasks.

## Conventions

**Naming:** React components are likely PascalCase (inferred from `DashboardPage.css`).
**Structure:** Standard `create-react-app` structure with separated `pages`, `components`, and `utils`.
**Testing:** Basic Jest setup included (`@testing-library/*` dependencies in `package.json`), but test coverage is unknown.
